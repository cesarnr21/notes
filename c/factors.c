
/* The pupose of the program is to find all of the factors of a non negative integer input */

#include <stdio.h>

int main() {
    int number;
    int results[];
    printf("Enter a non negative interger: ");
    scanf("%d", &number);
    while (number < 1) {
        printf("\nPlease enter a Non-negative Integer Number: ");
    }
    factor(number);
    printf("\nThe factors of %i are: \n", number);
    for (results[]){

    }
    return 0;
}

int factor(int number) {
    int i = 
    while (number > 1) {
        number = 
    }
}


